function updateProgress(percent) {
    document.getElementById('progress').style.width = percent + '%';
  }
  
  // Example usage:
  // Call updateProgress(percent) with the percentage of progress
  updateProgress(80);
  